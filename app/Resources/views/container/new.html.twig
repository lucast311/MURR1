{% extends 'base.html.twig' %}

{% block body %}
    <h1>Container creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="ui button" />
    {{ form_end(form) }}


       <a href="{{ path('container_search') }}" class="ui button">Search</a>
{% endblock %}
